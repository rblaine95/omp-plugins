{
  "name": "@rblaine95/omp-plugins",
  "version": "1.1.0",
  "private": true,
  "description": "rblaine95 personal oh-my-pi extensions, installable via `omp plugin install`.",
  "license": "MIT",
  "workspaces": [
    "extensions/*"
  ],
  "scripts": {
    "check": "biome check",
    "clean": "rm -rf node_modules",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.11",
    "@oh-my-pi/pi-coding-agent": "^18.0.11",
    "@types/bun": "1.4.0",
    "sort-package-json": "^4.0.0",
    "typescript": "^7.0.2"
  },
  "packageManager": "bun@1.4.0",
  "omp": {
    "features": {
      "rules-guard": {
        "description": "Enforce the Claude permissions allow/deny policy across all omp tools (read/write/edit/find/search/bash/eval/browser), and redact secret-shaped text from what the model sees, including output from ! and $ commands.",
        "default": true,
        "extensions": [
          "./extensions/rules-guard/index.ts"
        ]
      },
      "usage-status": {
        "description": "Show remaining subscription usage and reset countdowns for every provider, in a color-coded row above the omp editor.",
        "default": true,
        "extensions": [
          "./extensions/usage-status/index.ts"
        ]
      }
    }
  }
}
